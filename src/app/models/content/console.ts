import { Timestamp } from 'rxjs/internal/operators/timestamp';

export interface Console {
    generation : number,
    name : string,
    qualifiedName : string,
    released : string,
    type : string,
    maker : string
}