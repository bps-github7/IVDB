import { DisplayNameValidationDirective } from './display-name-validation.directive';

describe('DisplayNameValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayNameValidationDirective();
    expect(directive).toBeTruthy();
  });
});
