import { AuthService } from '../../core/auth.service';
import { DisplayNameUniqueValidationDirective } from './display-name-unique-validation.directive';

describe('DisplayNameValidationDirective', () => {
  it('should create an instance', () => {
    // const directive = new DisplayNameUniqueValidationDirective();
    // expect(directive).toBeTruthy();
  });
});
